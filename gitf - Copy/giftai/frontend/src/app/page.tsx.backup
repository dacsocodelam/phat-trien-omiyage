"use client";
import { useState } from "react";
import axios from "axios";
import { useTranslation } from "react-i18next";
import Header from "../components/Header";
import Footer from "../components/Footer";
import HowItWorks from "../components/HowItWorks";
import BlogSection from "../components/BlogSection";

export default function Home() {
                    </div>
                    
                    {/* Orbiting Elements */}
                    <div className="absolute inset-0 animate-spin" style={{animationDuration: '4s'}}>
                      <div className="absolute top-0 left-1/2 transform -translate-x-1/2 text-2xl">💫</div>
                    </div>
                    <div className="absolute inset-0 animate-spin" style={{animationDuration: '3s', animationDirection: 'reverse'}}>
                      <div className="absolute bottom-0 left-1/2 transform -translate-x-1/2 text-2xl">✨</div>
                    </div>
                    <div className="absolute inset-0 animate-spin" style={{animationDuration: '5s'}}>
                      <div className="absolute left-0 top-1/2 transform -translate-y-1/2 text-2xl">⭐</div>
                    </div>
                    <div className="absolute inset-0 animate-spin" style={{animationDuration: '3.5s', animationDirection: 'reverse'}}>
                      <div className="absolute right-0 top-1/2 transform -translate-y-1/2 text-2xl">🌟</div>
                    </div>
                  </div>
                  
                  {/* Floating Hearts */}
                  <div className="absolute top-8 left-8 animate-pulse text-xl delay-75">💖</div>
                  <div className="absolute top-12 right-12 animate-pulse text-xl delay-150">💝</div>
                  <div className="absolute bottom-8 left-12 animate-pulse text-xl delay-300">🎀</div>unction Home() {
  const { t } = useTranslation();
  const [formData, setFormData] = useState({
    age: "",
    gender: "女性",
    relationship: "",
    hobby: "",
    budget: "",
    occasion: "",
  });
  const [suggestions, setSuggestions] = useState("");
  const [results, setResults] = useState([]);
  const [error, setError] = useState("");
  const [isLoading, setIsLoading] = useState(false);
  const [loadingMessage, setLoadingMessage] = useState("");

  const handleChange = (
    e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>
  ) => {
    setFormData({ ...formData, [e.target.name]: e.target.value });
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError("");
    setSuggestions("");
    setResults([]);
    setIsLoading(true);
    
    // Các thông báo loading động
    const loadingMessages = [
      "🤖 AI đang phân tích thông tin của bạn...",
      "🔍 Đang tìm kiếm trong hàng nghìn sản phẩm...",
      "💡 Đang so sánh và lựa chọn những món quà tốt nhất...", 
      "🎯 Đang tùy chỉnh gợi ý theo sở thích riêng...",
      "✨ Sắp hoàn thành! Đang tạo danh sách quà hoàn hảo..."
    ];
    
    let messageIndex = 0;
    setLoadingMessage(loadingMessages[0]);
    
    // Thay đổi thông báo mỗi 2 giây
    const messageInterval = setInterval(() => {
      messageIndex = (messageIndex + 1) % loadingMessages.length;
      setLoadingMessage(loadingMessages[messageIndex]);
    }, 2000);
    
    try {
      const res = await axios.get("http://localhost:3000/api/suggest", {
        params: formData,
      });
      
      clearInterval(messageInterval);
      setLoadingMessage("🎉 Hoàn thành! Đang hiển thị kết quả...");
      
      // Delay nhỏ để người dùng thấy thông báo hoàn thành
      setTimeout(() => {
        setSuggestions(res.data.suggestions);
        setResults(res.data.products);
        setIsLoading(false);
      }, 1000);
      
    } catch (err) {
      clearInterval(messageInterval);
      setIsLoading(false);
      setError("❌ Đã có lỗi xảy ra. Vui lòng thử lại sau!");
    }
  };

  return (
    <>
      <Header />
      <main id="home">
        <div className="min-h-screen bg-gradient-to-br from-[#FFFDD0] to-[#F0F8FF] text-[#001f3f]">
          {/* Hero Section */}
          <div className="relative overflow-hidden bg-gradient-to-r from-[#001f3f] to-[#003366] text-white">
            <div className="absolute inset-0 bg-black opacity-20"></div>
            <div className="relative z-10 max-w-6xl mx-auto px-4 py-16 md:py-24">
              <div className="text-center">
                <div className="flex justify-center items-center gap-3 mb-6">
                  <div className="relative">
                    <svg
                      width="48"
                      height="48"
                      viewBox="0 0 24 24"
                      fill="none"
                      xmlns="http://www.w3.org/2000/svg"
                      className="text-[#FFD700]"
                    >
                      <path
                        d="M12 2L15.09 8.26L22 9.27L17 14.14L18.18 21.02L12 17.77L5.82 21.02L7 14.14L2 9.27L8.91 8.26L12 2Z"
                        fill="currentColor"
                      />
                    </svg>
                    <div className="absolute -top-1 -right-1 w-4 h-4 bg-[#FFD700] rounded-full animate-pulse"></div>
                  </div>
                  <h1 className="text-5xl md:text-6xl font-bold bg-gradient-to-r from-[#FFD700] to-[#FFA500] bg-clip-text text-transparent">
                    GiftAI
                  </h1>
                </div>
                <h2 className="text-2xl md:text-3xl font-semibold mb-4 text-[#FFD700]">
                  Chuyên gia quà tặng cá nhân của bạn
                </h2>
                <p className="text-xl md:text-2xl mb-8 text-gray-200 max-w-3xl mx-auto leading-relaxed">
                  🎁{" "}
                  <strong>
                    Đừng để sinh nhật người thương thành cơn ác mộng!
                  </strong>
                  <br />
                  Tìm món quà hoàn hảo chỉ trong{" "}
                  <span className="text-[#FFD700] font-bold">60 giây</span> với
                  AI
                </p>
                <div className="bg-white/10 backdrop-blur-sm rounded-2xl p-6 max-w-2xl mx-auto mb-8">
                  <div className="flex items-center justify-center gap-4 text-sm md:text-base">
                    <div className="flex items-center gap-2">
                      <div className="w-3 h-3 bg-green-400 rounded-full animate-pulse"></div>
                      <span>Bận rộn? Không sao!</span>
                    </div>
                    <div className="flex items-center gap-2">
                      <div className="w-3 h-3 bg-blue-400 rounded-full animate-pulse"></div>
                      <span>AI hiểu bạn</span>
                    </div>
                    <div className="flex items-center gap-2">
                      <div className="w-3 h-3 bg-[#FFD700] rounded-full animate-pulse"></div>
                      <span>Quà chuẩn 100%</span>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <div className="absolute bottom-0 left-0 right-0 h-20 bg-gradient-to-t from-[#FFFDD0] to-transparent"></div>
          </div>

          {/* Main Content */}
          <div className="max-w-6xl mx-auto px-4 py-12">
            {/* CTA Section */}
            <div className="text-center mb-12">
              <h3 className="text-3xl md:text-4xl font-bold mb-6 text-[#001f3f]">
                🚀 Bắt đầu tìm quà ngay!
              </h3>
              <p className="text-lg text-gray-600 mb-8 max-w-2xl mx-auto">
                Chỉ cần 5 thông tin đơn giản, AI sẽ gợi ý những món quà{" "}
                <strong>được đảm bảo</strong> làm người thương của bạn hạnh phúc
              </p>
            </div>

            {/* AI Form - Redesigned for better UX */}
            <form
              onSubmit={handleSubmit}
              className="max-w-2xl mx-auto space-y-6 bg-white p-8 rounded-3xl shadow-2xl border border-[#FFD700]/20"
            >
              <div className="text-center mb-6">
                <h4 className="text-xl font-semibold text-[#001f3f] mb-2">
                  💝 Thông tin người nhận quà
                </h4>
                <p className="text-sm text-gray-500">
                  Càng chi tiết, món quà càng phù hợp
                </p>
              </div>

              <div className="grid md:grid-cols-2 gap-4">
                <div className="space-y-2">
                  <label className="block text-sm font-medium text-[#001f3f]">
                    👤 Tuổi
                  </label>
                                  <input
                  name="age"
                  placeholder="VD: 28"
                  value={formData.age}
                  onChange={handleChange}
                  disabled={isLoading}
                  className={`border-2 p-3 w-full rounded-xl focus:outline-none focus:ring-2 focus:ring-[#FFD700] focus:border-[#FFD700] transition-all ${
                    isLoading 
                      ? "bg-gray-100 border-gray-300 cursor-not-allowed" 
                      : "border-[#001f3f]/20"
                  }`}
                  type="number"
                  required
                />
                </div>

                <div className="space-y-2">
                  <label className="block text-sm font-medium text-[#001f3f]">
                    ⚧ Giới tính
                  </label>
                                  <select
                  name="gender"
                  value={formData.gender}
                  onChange={handleChange}
                  disabled={isLoading}
                  className={`border-2 p-3 w-full rounded-xl focus:outline-none focus:ring-2 focus:ring-[#FFD700] focus:border-[#FFD700] transition-all ${
                    isLoading 
                      ? "bg-gray-100 border-gray-300 cursor-not-allowed" 
                      : "border-[#001f3f]/20"
                  }`}
                >
                    <option value="女性">👩 Nữ</option>
                    <option value="男性">👨 Nam</option>
                  </select>
                </div>
              </div>

              <div className="space-y-2">
                <label className="block text-sm font-medium text-[#001f3f]">
                  💕 Mối quan hệ
                </label>
                <input
                  name="relationship"
                  placeholder="VD: Bạn gái, vợ, mẹ, bạn thân, đồng nghiệp..."
                  value={formData.relationship}
                  onChange={handleChange}
                  disabled={isLoading}
                  className={`border-2 p-3 w-full rounded-xl focus:outline-none focus:ring-2 focus:ring-[#FFD700] focus:border-[#FFD700] transition-all ${
                    isLoading 
                      ? "bg-gray-100 border-gray-300 cursor-not-allowed" 
                      : "border-[#001f3f]/20"
                  }`}
                  required
                />
              </div>

              <div className="space-y-2">
                <label className="block text-sm font-medium text-[#001f3f]">
                  🎨 Sở thích & Tính cách
                </label>
                <input
                  name="hobby"
                  placeholder="VD: Du lịch, đọc sách, yoga, nấu ăn, công nghệ, thời trang..."
                  value={formData.hobby}
                  onChange={handleChange}
                  disabled={isLoading}
                  className={`border-2 p-3 w-full rounded-xl focus:outline-none focus:ring-2 focus:ring-[#FFD700] focus:border-[#FFD700] transition-all ${
                    isLoading 
                      ? "bg-gray-100 border-gray-300 cursor-not-allowed" 
                      : "border-[#001f3f]/20"
                  }`}
                  required
                />
              </div>

              <div className="grid md:grid-cols-2 gap-4">
                <div className="space-y-2">
                  <label className="block text-sm font-medium text-[#001f3f]">
                    💰 Ngân sách (VNĐ)
                  </label>
                  <input
                    name="budget"
                    placeholder="VD: 500000"
                    value={formData.budget}
                    onChange={handleChange}
                    disabled={isLoading}
                    className={`border-2 p-3 w-full rounded-xl focus:outline-none focus:ring-2 focus:ring-[#FFD700] focus:border-[#FFD700] transition-all ${
                      isLoading 
                        ? "bg-gray-100 border-gray-300 cursor-not-allowed" 
                        : "border-[#001f3f]/20"
                    }`}
                    type="number"
                    required
                  />
                </div>

                <div className="space-y-2">
                  <label className="block text-sm font-medium text-[#001f3f]">
                    🎉 Dịp đặc biệt
                  </label>
                  <input
                    name="occasion"
                    placeholder="VD: Sinh nhật, Valentine, kỷ niệm..."
                    value={formData.occasion}
                    onChange={handleChange}
                    disabled={isLoading}
                    className={`border-2 p-3 w-full rounded-xl focus:outline-none focus:ring-2 focus:ring-[#FFD700] focus:border-[#FFD700] transition-all ${
                      isLoading 
                        ? "bg-gray-100 border-gray-300 cursor-not-allowed" 
                        : "border-[#001f3f]/20"
                    }`}
                    required
                  />
                </div>
              </div>

          <button
            type="submit"
            disabled={isLoading}
            className={`w-full font-bold p-4 rounded-xl text-lg transition-all duration-300 transform shadow-lg ${
              isLoading
                ? "bg-gray-400 text-gray-700 cursor-not-allowed"
                : "bg-gradient-to-r from-[#FFD700] to-[#FFA500] text-[#001f3f] hover:from-[#001f3f] hover:to-[#003366] hover:text-white hover:scale-105"
            }`}
          >
            {isLoading ? (
              <div className="flex items-center justify-center gap-3">
                <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-[#001f3f]"></div>
                <span>Đang xử lý...</span>
              </div>
            ) : (
              "🎯 Tìm quà hoàn hảo ngay!"
            )}
          </button>              <p className="text-xs text-center text-gray-400 mt-4">
                ⏱️ Chỉ mất 60 giây • 🔒 Bảo mật tuyệt đối • 💯 Miễn phí hoàn
                toàn
              </p>
            </form>

            {/* Loading Display */}
            {isLoading && (
              <div className="max-w-2xl mx-auto mt-12">
                <div className="bg-gradient-to-br from-white to-[#FFD700]/10 p-8 rounded-3xl shadow-2xl border border-[#FFD700]/30 text-center">
                  {/* Main Loading Animation */}
                  <div className="relative mb-6">
                    <div className="w-24 h-24 mx-auto">
                      {/* Rotating Gift Box */}
                      <div className="animate-spin text-6xl">🎁</div>
                      {/* Spinning Ring */}
                      <div className="absolute inset-0 border-4 border-[#FFD700]/30 border-t-[#FFD700] rounded-full animate-spin"></div>
                    </div>
                    {/* Floating Stars */}
                    <div className="absolute top-0 left-1/4 animate-bounce text-2xl">⭐</div>
                    <div className="absolute top-4 right-1/4 animate-bounce text-2xl delay-150">✨</div>
                    <div className="absolute bottom-4 left-1/3 animate-bounce text-2xl delay-300">💝</div>
                  </div>

                  {/* Dynamic Loading Message */}
                  <h3 className="text-xl font-bold text-[#001f3f] mb-4">
                    AI đang làm việc cho bạn!
                  </h3>
                  <p className="text-lg text-gray-700 mb-6 min-h-[28px] transition-all duration-500">
                    {loadingMessage}
                  </p>

                  {/* Progress Steps */}
                  <div className="flex justify-center space-x-2 mb-6">
                    {[1, 2, 3, 4, 5].map((step) => (
                      <div
                        key={step}
                        className={`w-3 h-3 rounded-full transition-all duration-500 ${
                          loadingMessage.includes("phân tích") && step === 1 ? "bg-[#FFD700] animate-pulse" :
                          loadingMessage.includes("tìm kiếm") && step === 2 ? "bg-[#FFD700] animate-pulse" :
                          loadingMessage.includes("so sánh") && step === 3 ? "bg-[#FFD700] animate-pulse" :
                          loadingMessage.includes("tùy chỉnh") && step === 4 ? "bg-[#FFD700] animate-pulse" :
                          loadingMessage.includes("hoàn thành") && step === 5 ? "bg-[#FFD700] animate-pulse" :
                          "bg-gray-300"
                        }`}
                      />
                    ))}
                  </div>

                  {/* Fun Facts */}
                  <div className="bg-white/80 rounded-2xl p-4 text-sm text-gray-600">
                    <p className="font-medium mb-2">💡 <strong>Bạn có biết?</strong></p>
                    <p>
                      AI của chúng tôi phân tích hơn <span className="text-[#FFD700] font-bold">10,000</span> sản phẩm 
                      và <span className="text-[#FFD700] font-bold">50+</span> yếu tố tâm lý để tìm món quà hoàn hảo cho bạn!
                    </p>
                  </div>
                </div>
              </div>
            )}

            {/* Error Display */}
            {error && (
              <div className="max-w-2xl mx-auto mt-6 p-4 bg-red-50 border border-red-200 rounded-xl">
                <p className="text-red-600 text-center">❌ {error}</p>
              </div>
            )}

            {/* AI Suggestions Display */}
            {suggestions && (
              <div className="max-w-4xl mx-auto mt-12 bg-gradient-to-br from-white to-[#FFD700]/5 p-8 rounded-3xl shadow-2xl border border-[#FFD700]/30">
                <div className="text-center mb-6">
                  <h2 className="text-3xl font-bold mb-4 bg-gradient-to-r from-[#FFD700] to-[#FFA500] bg-clip-text text-transparent">
                    🎁 Gợi ý từ chuyên gia AI
                  </h2>
                  <p className="text-sm text-gray-500">
                    Được phân tích dựa trên sở thích và ngân sách của bạn
                  </p>
                </div>
                <div className="bg-white/80 p-6 rounded-2xl shadow-inner">
                  <p className="whitespace-pre-wrap text-justify leading-relaxed text-[#001f3f] text-lg">
                    {suggestions}
                  </p>
                </div>
              </div>
            )}

            {/* Products Display */}
            {results.length > 0 && (
              <div className="max-w-6xl mx-auto mt-12">
                <h3 className="text-2xl font-bold text-center mb-8 text-[#001f3f]">
                  🛍️ Sản phẩm được AI gợi ý
                </h3>
                <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                  {results.map((prod: any, index: number) => (
                    <div
                      key={index}
                      className="bg-white p-6 rounded-2xl shadow-lg hover:shadow-2xl hover:scale-105 transition-all duration-300 border border-[#FFD700]/20"
                    >
                      <div className="relative mb-4">
                        <img
                          src="https://images.unsplash.com/photo-1549465220-1a8b9238cd48?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1000&q=80"
                          alt={prod.name}
                          className="w-full h-48 object-cover rounded-xl"
                        />
                        <div className="absolute top-3 right-3 bg-[#FFD700] text-[#001f3f] px-3 py-1 rounded-full text-sm font-bold">
                          AI Pick ⭐
                        </div>
                      </div>
                      <h3 className="text-xl font-semibold mb-3 text-[#001f3f]">
                        {prod.name}
                      </h3>
                      <p className="text-sm text-gray-600 mb-4 line-clamp-3">
                        {prod.description}
                      </p>
                      <div className="flex justify-between items-center mb-4">
                        <p className="font-bold text-lg text-[#001f3f]">
                          💰 {prod.price?.toLocaleString()}₫
                        </p>
                        <div className="bg-green-100 text-green-800 px-2 py-1 rounded-full text-xs">
                          ✓ Phù hợp
                        </div>
                      </div>
                      <a
                        href={prod.url}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="block w-full bg-gradient-to-r from-[#FFD700] to-[#FFA500] text-[#001f3f] font-bold py-3 rounded-xl text-center hover:from-[#001f3f] hover:to-[#003366] hover:text-white transition-all duration-300 transform hover:scale-105"
                      >
                        🛒 Mua ngay
                      </a>
                    </div>
                  ))}
                </div>
              </div>
            )}

            {/* Trust Indicators */}
            <div className="max-w-4xl mx-auto mt-16 text-center">
              <h4 className="text-xl font-semibold mb-6 text-[#001f3f]">
                ⭐ Tại sao chọn GiftAI?
              </h4>
              <div className="grid md:grid-cols-3 gap-6">
                <div className="bg-white p-6 rounded-2xl shadow-lg">
                  <div className="text-4xl mb-4">🤖</div>
                  <h5 className="font-bold mb-2 text-[#001f3f]">
                    AI Thông minh
                  </h5>
                  <p className="text-sm text-gray-600">
                    Phân tích hàng nghìn sản phẩm để tìm món quà hoàn hảo
                  </p>
                </div>
                <div className="bg-white p-6 rounded-2xl shadow-lg">
                  <div className="text-4xl mb-4">⚡</div>
                  <h5 className="font-bold mb-2 text-[#001f3f]">Siêu nhanh</h5>
                  <p className="text-sm text-gray-600">
                    Chỉ 60 giây để có gợi ý quà tặng chuẩn xịn
                  </p>
                </div>
                <div className="bg-white p-6 rounded-2xl shadow-lg">
                  <div className="text-4xl mb-4">💝</div>
                  <h5 className="font-bold mb-2 text-[#001f3f]">
                    Đảm bảo hài lòng
                  </h5>
                  <p className="text-sm text-gray-600">
                    Món quà được chọn dựa trên khoa học & cảm xúc
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>

      <HowItWorks />
      <BlogSection />
      <Footer />
    </>
  );
}
